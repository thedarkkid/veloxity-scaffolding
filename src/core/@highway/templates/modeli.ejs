// Model Interface file generated with Highway-CLI.
import {Document} from "mongoose";

export default interface I<%=modeliName%>Model extends Document {
    column?:string; // <-column->?:<-property->
    created_at?:Date;
    updated_at?:Date;
}
