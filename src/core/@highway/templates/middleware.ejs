// Middleware file generated with Highway-CLI.

import express from "express";

const <%=functionName%> = (req:express.Request, res:express.Response, next:express.NextFunction):void => {
    // middleware logic
    next();
};

export default <%=functionName%>;